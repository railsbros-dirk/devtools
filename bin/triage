#!/usr/bin/env ruby
# encoding: utf-8

require 'bundler'

Bundler.setup

require 'triage'

command = ARGV.first

case command
when 'init'   then Triage.init
when 'sync'   then Triage.sync
when 'update' then Triage.update
else
  puts 'command not supported'
end
